<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="uk.co.threeequals.notepad">
    <application android:icon="@drawable/icon" android:theme="@style/LightTheme">
        <uses-sdk android:minSdkVersion="7"
            android:targetSdkVersion="21" />
        <activity android:theme="@style/Theme.AppCompat.Light" android:name=".Notepad" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="uk.co.threeequals.notepad.NoteEdit" />
        <service android:name=".ScheduleService" />
        <service android:name=".NotifyService" />
    </application>
</manifest>